{
  "input_files": [
    {
      "name": "yield_opportunities.csv",
      "description": "Current yield data input for web application",
      "data_type": {
        "interface": "YieldOpportunity",
        "properties": {
          "name": "string",
          "estimatedApy": "number",
          "network": "string",
          "tvl": "number",
          "relativeRisk": ["Low", "Medium", "High"],
          "notes": "string",
          "category": ["stablecoin", "volatileAsset", "advancedStrategies"],
          "link": "string",
          "isBenchmark": "boolean",
          "dateAdded": "admin.firestore.Timestamp"
        }
      }
    },
    {
      "name": "yields.csv",
      "description": "DeFiLlama data for stablecoin and volatileAsset categories"
    }
  ],
  "task": {
    "action": "analyze_and_generate",
    "output": {
      "name": "yield_opportunities.csv",
      "format": "CSV",
      "requirements": [
        "Conform to the exact format of the existing CSV",
        "Include best opportunities based on APY and TVL",
        "Compatible with existing categories and format",
        "Do not include any existing opportunities in new output",
        "Only include the top 3 opportunities for each category",
        "Only include the notes if its to do with a lockup or other important detail",
        "The Id field should be all be blank as a new one will be generated by firebase",
        "Format name as '<Opportunity> on <Project>'"
      ]
    }
  },
  "additional_instructions": "Reply with only the CSV file attached, no other files or text."
}
